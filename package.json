{
  "name": "rest-api",
  "version": "1.0.0",
  "description": "Demo project for a simple prod ready node REST API",
  "scripts": {
    "build": "tsc",
    "start-server": "node ./dist/server.js",
    "clean": "rimraf ./dist",
    "poststart-server" : "npm run clean"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  }
}
